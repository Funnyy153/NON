# ✅ การตั้งค่าเสร็จสมบูรณ์

## สิ่งที่ทำแล้ว:

1. ✅ สร้างไฟล์ `.env.local` พร้อม Web App URL
2. ✅ ตั้งค่า `GOOGLE_APPS_SCRIPT_URL`

## ⚠️ สิ่งที่ต้องทำต่อ:

### 1. แก้ไข Google Apps Script (ถ้ายังไม่ได้ทำ)

เนื่องจากยังมี error "Script function not found: doGet" คุณต้อง:

1. เปิด Google Sheet
2. ไปที่ **Extensions** > **Apps Script**
3. ตรวจสอบว่ามี function `doGet` และ `doPost` อยู่แล้ว (ตามโค้ดใน `GOOGLE_APPS_SCRIPT_SETUP.md`)
4. **Deploy ใหม่**:
   - คลิก **Deploy** > **Manage deployments**
   - คลิก **Edit** (ไอคอนดินสอ) ข้าง deployment ที่มีอยู่
   - เลือก **Version: New version**
   - คลิก **Deploy**

### 2. Restart Development Server

```bash
# หยุด server (Ctrl+C)
# แล้วรันใหม่
npm run dev
```

### 3. ทดสอบ

1. เปิดเบราว์เซอร์ไปที่: `http://localhost:3000/pages/before`
2. กด checkbox "สถานะ" ในแถวใดแถวหนึ่ง
3. ตรวจสอบใน Google Sheet ว่าค่าถูกอัปเดตเป็น "1" หรือไม่

## 🔍 ตรวจสอบว่า Apps Script ทำงาน:

เปิด URL นี้ในเบราว์เซอร์:
```
https://script.google.com/macros/s/AKfycbzCxRXVyHg1YDQUWsFI_EbQEdQH6kZzR8QDKtrV00zDxoJhioOVyxYgFlrw5DeutgfSoA/exec
```

ควรเห็น:
```json
{
  "success": true,
  "message": "Google Apps Script is working!",
  "note": "Use POST request to update sheet data"
}
```

ถ้ายังเห็น error "doGet not found" แสดงว่ายังไม่ได้ deploy ใหม่

## 📝 หมายเหตุ:

- ไฟล์ `.env.local` จะไม่ถูก commit เข้า git (มีใน .gitignore แล้ว)
- ถ้า deploy ไปที่ production (Vercel/Netlify) ต้องตั้งค่า environment variable ใน dashboard ของ hosting service

